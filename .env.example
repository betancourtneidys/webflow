# Optional. Without it the assistant answers from the scenario data (demo mode).
# On Webflow Cloud, add it as a Secret Variable in the environment settings.
ANTHROPIC_API_KEY=

# Optional model override.
# CLOUD_DETECTIVE_MODEL=claude-opus-5
